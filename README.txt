The Javadocs for the API and the implementation is found in APIdoc in the FinalProject directory.

The UML diagram for the API and implementation is titled "InterfacesAndImplementation.uml" in the src directory.

Our actual code doesn't work yet, had trouble building the MVC in such little time. If we had another day we would
have been able to meet the requirements in full, but we weren't the testers for our group so we weren't testing
our API from the beginning.